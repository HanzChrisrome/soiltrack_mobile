import 'package:flutter/material.dart';

class AddCustomCrop extends StatelessWidget {
  const AddCustomCrop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Custom Crop'),
      ),
    );
  }
}
